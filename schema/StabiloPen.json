{
    "failures": [],
    "$schema": "http://json-schema.org/draft-07/schema#",
    "examples": [

    ],
    "additionalProperties": false,
    "title": "StabiloPen",
    "type": "object",
    "properties": {
        "wordList": {
            "description": "last words written",
            "readOnly": true,
            "title": "wordlist",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "text": {
            "description": "last words written as a string",
            "readOnly": true,
            "title": "text",
            "type": "string"
        },
        "batteryLevel": {
            "unit": "%",
            "maximum": 100,
            "description": "Battery level where 100% is a fully charged",
            "readOnly": true,
            "title": "batteryLevel",
            "type": "number",
            "minimum": 0
        }
    },
    "$id": "https://kosmos-lab.de/schema/StabiloPen.json"
}